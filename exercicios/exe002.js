let num = [2,4,1,5,8,3,7]
num.push(80)

for(let pos=0;pos<num.length;pos++){
    console.log(`posicao ${pos} tem o valor ${num[pos]}`)
}
console.log(`existem ${num.length} numeros`)

/*
for( let pos in num){
    console.log(` posicao ${pos} tem o valor ${num[pos]}`)
}

*/

